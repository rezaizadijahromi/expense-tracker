import axios from "axios";

const signin = async () => {
  // try {
  //   let response = await axios.post("/api/users/login", user);
  //   return response;
  // } catch (error) {}
};

export { signin };
